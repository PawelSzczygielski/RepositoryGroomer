<controls:MetroWindow x:Class="RepositoryGroomer.Modern.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:modern="clr-namespace:RepositoryGroomer.Modern"
        mc:Ignorable="d"
        Title="MainWindowView" Height="350" Width="525">
    <Window.DataContext>
        <modern:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Code repository location: " Margin="5"/>
            <TextBox Grid.Column="1" Margin="5"/>
            <Button Grid.Column="2" Content="..." Width="30" Margin="5"/>
        </Grid>

        <TabControl Grid.Row="1">
            <TabItem Header="Projects with links">
                <TreeView ItemsSource="{Binding Projects}">
                    <TreeView.ItemTemplate>
                        <DataTemplate>
                            <TreeViewItem  ItemsSource="{Binding Links}">
                                <TreeViewItem.Header>
                                    <Grid Width="350">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding FilePath}" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0"/>
                                        <TextBlock Text="{Binding DirectoryPath}" Margin="10,0,10,0" HorizontalAlignment="Left" VerticalAlignment="Center"  Grid.Column="1"/>
                                        <TextBlock Text="{Binding IsProjectFileValid}" HorizontalAlignment="Left"  VerticalAlignment="Center"  Grid.Column="2"/>
                                    </Grid>
                                </TreeViewItem.Header>
                                <TreeViewItem.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="-20,0,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="AUto"></ColumnDefinition>
                                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            </Grid.ColumnDefinitions>
                                            <!--<TextBlock Text="{Binding Name}" Margin="10,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0"/>-->
                                            <TextBlock Text="{Binding LinkedFileUnwrappedPath}" Margin="10,0,10,0" HorizontalAlignment="Left" VerticalAlignment="Center"  Grid.Column="1"/>
                                            <TextBlock Text="{Binding IsLinkValid}" HorizontalAlignment="Left" VerticalAlignment="Center"  Grid.Column="2"/>
                                        </Grid>
                                    </DataTemplate>
                                </TreeViewItem.ItemTemplate>
                            </TreeViewItem>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </TabItem>
            <TabItem Header="Others" />
        </TabControl>
    </Grid>
</controls:MetroWindow>
